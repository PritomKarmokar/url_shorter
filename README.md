## URL_SHORTER
-  just trying to implement `url_shortener` from `first principles` thought
- applying basic knowledge of how should a simple `url shortener` should work
- then expand it (Improve it) :(